Mirror of `imagine-computer-web/packages/utils/src`, aliased as `@repo/utils/*`.

Only what this app uses is mirrored — currently `base-button/`, the `cva` base the `@repo/ui`
Button builds its variants on.

Kept byte-identical to the monorepo source so it can be refreshed with a plain `cp` and
deleted on port-back, when the real workspace package takes over.

No deviations.

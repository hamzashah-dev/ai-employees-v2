Mirror of `imagine-computer-web/packages/types/src`, aliased as `@repo/types/*`.

Kept byte-identical to the monorepo source so it can be refreshed with a plain `cp`
and deleted outright when this app is ported back in (the real package takes over).
Only the modules this app actually needs are mirrored.
